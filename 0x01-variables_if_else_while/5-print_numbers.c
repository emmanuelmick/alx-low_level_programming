#include <stdio.h>

/**
 * main - prints all base 10 numbers beginning from zero
 * Return: Always 0
 */
int main(void)
{
int num;
for (num = 0; num < 10; num++)
{
	printf("%d ", num);
}
return (0);
}
